// https://leetcode.cn/problems/bu-ke-pai-zhong-de-shun-zi-lcof/
class Solution {

    /**
     * 思路：排序，计算非0的数字，差值的sum，如果比0的数量少，则0足够补在中间，保证连续
     * 
     * 这个题目描述有点垃圾，没有说清楚连续是指从小到大连续，还是乱序连续
     * 并且2个文物是同一个朝代的case，他也认为是不连续
     * [0,0,2,2,5] 例如这个case其实可以认为是22345 算连续还是不连续？
     */
    public boolean checkDynasty(int[] places) {
        Arrays.sort(places);
        int zeroCnt = 0;
        for (int i = 0; i < places.length; i++) {
            if (places[i] == 0) {
                zeroCnt++;
            }
        }

        int gapSum = 0;
        for (int i = places.length - 1; i >= 1; i--) {
            if (places[i - 1] == 0) {
                break;
            }
            if (places[i] == places[i - 1]) {
                return false;
            }
            gapSum += places[i] - places[i - 1] - 1;
        }

        return gapSum <= zeroCnt;
    }
}
